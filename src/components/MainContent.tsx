import React from 'react';

const MainContent: React.FC = () => {
  return (
    <main className="mt-10">
      {/* Main content goes here */}
      {/* For example, a section for generating datasets */}
    </main>
  );
};

export default MainContent;
